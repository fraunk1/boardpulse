__pycache__/
*.pyc
*.pyo
venv/
.venv/
*.db
*.db-wal
*.db-shm
data/screenshots/
data/documents/
data/reports/
.env
*.egg-info/
dist/
build/
data/reports/
data/screenshots/
data/documents/
data/exhibits/
data/_research/
data/_verify/
data/cache/
data/logs/
# eval-harness candidate runs are throwaway; the frozen gold set in
# data/gold/ is COMMITTED on purpose — do not add an ignore rule for it
data/eval/
_targets.md
# the artifact publish pointer IS committed — later sessions must republish to
# the same claude.ai URL instead of minting a new artifact (last match wins,
# so this unignore chain must stay BELOW the data/reports/ ignore lines)
!data/reports/
data/reports/*
!data/reports/artifact/
data/reports/artifact/*
!data/reports/artifact/ARTIFACT.json
